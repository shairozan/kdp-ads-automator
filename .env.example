# ===========================================
# PostgreSQL Database
# ===========================================
# For Docker: uses internal networking
# For local dev: use your local PostgreSQL
DATABASE_URL=postgresql://kdp:kdp_secret@localhost:5432/kdp_ads

# Password for PostgreSQL (used by docker-compose)
POSTGRES_PASSWORD=kdp_secret

# ===========================================
# Amazon Advertising API Credentials
# ===========================================
# Get these from https://advertising.amazon.com/API/docs/en-us/setting-up/step-1-create-a-developer-account
# These are OPTIONAL - you can use CSV imports without API access

AMAZON_CLIENT_ID=your_client_id_here
AMAZON_CLIENT_SECRET=your_client_secret_here
AMAZON_REFRESH_TOKEN=your_refresh_token_here

# Amazon Advertising Profile ID
# This is obtained after OAuth and selecting your advertising profile
AMAZON_PROFILE_ID=your_profile_id_here

# ===========================================
# Book Configuration (for ROI calculations)
# ===========================================
# Royalty per sale in USD (e.g., if you get $2.80 per Kindle sale)
BOOK_ROYALTY_AMOUNT=2.80

# Optional: KENP royalty rate per page (for Kindle Unlimited)
# Average is around $0.004-0.005 per page read
KENP_RATE_PER_PAGE=0.0045

# ===========================================
# Sync Worker Configuration
# ===========================================
# How often to sync data from Amazon API (in minutes)
SYNC_INTERVAL_MINUTES=60

# How many days of historical metrics to fetch
METRICS_LOOKBACK_DAYS=14

# ===========================================
# Open WebUI + LiteLLM (Optional Web Interface)
# ===========================================
# Start with: docker compose --profile webui up -d

# Anthropic API key for Claude models
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# LiteLLM master key (used for API authentication between services)
LITELLM_MASTER_KEY=changeme

# Open WebUI secret key (required for session persistence)
WEBUI_SECRET_KEY=change-this-secret-key
